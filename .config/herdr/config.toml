onboarding = false

[theme]
name = "catppuccin"

[terminal]
new_cwd = "follow"

[keys]
prefix = "alt+f"
reload_config = "prefix+r"
previous_tab = "alt+j"
next_tab = "alt+k"
new_tab = ["ctrl+t", "cmd+t"]
close_pane = "ctrl+w"
close_tab = "cmd+w"
open_notification_target = "alt+h"
new_workspace = "cmd+n"
last_pane = ["alt+l", "cmd+l"]
goto = "cmd+slash"
previous_workspace = "cmd+shift+j"
next_workspace = "cmd+shift+k"
next_agent = "cmd+h"
rename_tab = "prefix+comma"
rename_workspace = "prefix+$"
toggle_sidebar = "cmd+b"
# wt owns the worktree flow (bare-repo layout under $REPOS_DIR), so herdr's
# native new_worktree stays unbound rather than offering a second, divergent one.
new_worktree = ""

[keys.indexed]
agents = "alt"

[[keys.command]]
key = "alt+g"
type = "pane"
command = "lazygit"
description = "lazygit"

# herdr-native port of switch-session.sh — fuzzy-pick/create a workspace
[[keys.command]]
key = "alt+s"
type = "pane"
command = "~/.herdr/switch-workspace.sh"
description = "switch workspace"

# wt worktree flow — bare-repo aware, not herdr's native new_worktree.
# "shell" is the only command type that can spawn a tab: pane and popup both stay
# inside the current tab, and there is no "tab" type. The wrapper creates the
# tab, runs the picker in it, and disposes of it afterwards.
[[keys.command]]
key = "cmd+shift+t"
type = "shell"
command = "~/.herdr/wt-tab.sh"
description = "wt open"

[ui]
mouse_capture = true
sidebar_collapsed_mode = "hidden"
hide_tab_bar_when_single_tab = true
pane_borders = true
pane_gaps = false
prompt_new_tab_name = false
agent_panel_sort = "priority"
tab_bar_position = "bottom"

[ui.toast]
delivery = "herdr"
delay_seconds = 1

[ui.sound]
enabled = false

[session]
resume_agents_on_restore = true

[experimental]
pane_history = true

